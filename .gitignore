# Firebase
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log
*-debug.log

# Firebase service account keys — NEVER commit these (full database access)
*serviceAccount*.json
*service-account*.json
*-firebase-adminsdk-*.json

# OS / editor noise
.DS_Store
Thumbs.db

# Transient tool caches
tools/.link-cache.json
tools/.spell-key-cache.json
